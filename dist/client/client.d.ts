import { type PEWS } from './pews';
import { type Phase } from '../types/pews';
import { Logger } from '../utils/logger';
export declare class PEWSClient {
    private readonly delay;
    private readonly Wrapper;
    readonly logger: Logger;
    protected HEADER_LEN: number;
    private _phase;
    private _cachedPhase;
    private staList;
    private tide;
    private needSync;
    private timeSyncIntervalID?;
    protected stopLoop: boolean;
    private eqkInfo?;
    constructor(wrapper: PEWS);
    get phase(): Phase;
    private escape;
    private startTimeSyncInterval;
    protected getTimeString(): string;
    private syncTime;
    private callback;
    protected getSta(url?: string): Promise<any>;
    private staBinStrHandler;
    protected getMMI(url?: string): Promise<any>;
    private eqkHandler;
    private mmiBinStrHandler;
    phaseHandler(): void;
    loop(): Promise<void>;
    run(): Promise<void>;
    stop(): void;
}
